import 'package:flutter/material.dart';

class WeatherSettings extends StatelessWidget {
  const WeatherSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Weather settings page'),
      ),
    );
  }
}
